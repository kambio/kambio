
ISSUER=passet_issuer 
SELECTOR=passet_choose 
TRANSFER=passet_transfer 

cd /home/jose/tmp/passet_test

DOM_1=dom01.com:9101
DOM_2=dom02.net:9102
DOM_3=dom03.gov:9103
DOM_4=dom04.org:9104

$ISSUER -k usu1 -cbx caja2
$ISSUER -k usu1 -cbx caja3
$ISSUER -k usu1 -cbx caja4

$TRANSFER -k usu1 -chann -r "$DOM_2"
$TRANSFER -k usu1 -chann -r "$DOM_3"
$TRANSFER -k usu1 -chann -r "$DOM_4"

$TRANSFER -k usu1 -cbx caja2 -r "$DOM_2"
$TRANSFER -k usu1 -cbx caja3 -r "$DOM_3"
$TRANSFER -k usu1 -cbx caja4 -r "$DOM_4"

$SELECTOR -k usu1 -pch
$SELECTOR -k usu1 -cn nm_usu2
$ISSUER -k usu1 -Dchn
$SELECTOR -k usu1 -pch
$TRANSFER -k usu1 -chann -r "$DOM_2"

$ISSUER -k usu1 -fbx caja2
$ISSUER -k usu1 -fbx caja3
$ISSUER -k usu1 -fbx caja4

$ISSUER -k usu1 -Ddbx caja2

$ISSUER -k usu1 -Dusr

